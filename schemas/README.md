# Schemas

JSON/YAML schemas para agent catalog, self-assessment e outros artefatos de governança.

- `agent-catalog.schema.json` — Schema do catálogo de agentes
- `self-assessment.schema.json` — Schema do formulário de auto-avaliação